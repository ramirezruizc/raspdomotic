# Ignorar directorio para web challenge de let's encrypt
certbot

# Ignorar dependencias de Node.js en client y server
client/node_modules/
server/node_modules/

# Ignorar archivos de entorno sensibles
.env
.env.local
.env.development
.env.production

# Ignorar archivos de salida en client (Vue, React, Nuxt)
client/dist/
client/.output/
client/.nuxt/

# Ignorar archivos de salida en server (logs, cachés, builds)
server/logs/
server/*.log
server/npm-debug.log*
server/yarn-debug.log*
server/yarn-error.log*

# Ignorar configuración de editores de código
.idea/
.vscode/
.DS_Store  # macOS
Thumbs.db  # Windows

# Ignorar archivos de dependencias de npm/yarn (raíz)
node_modules/
package-lock.json
yarn.lock

# Ignorar archivos específicos de Git
.git/

